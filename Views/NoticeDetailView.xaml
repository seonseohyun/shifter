<Page x:Class="ShifterUser.Views.NoticeDetailView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      FontFamily="Noto Sans KR"
      Width="400" Height="800"
      Background="White">

    <Page.Resources>
        <DropShadowEffect x:Key="ShadowEffect"
                          Color="Black"
                          ShadowDepth="5"
                          BlurRadius="10"
                          Opacity="0.5"/>
    </Page.Resources>

    <Grid>
        <!-- 상단바 -->
        <Border Background="#007AFF" Height="56" VerticalAlignment="Top">
            <Grid>
                <Button Content="＜"
                        Command="{Binding GoBackCommand}"
                        Width="40" Height="40"
                        Background="Transparent"
                        BorderBrush="Transparent"
                        FontSize="16"
                        FontWeight="Bold"
                        Foreground="White"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- 본문 -->
        <Grid Margin="16,6,16,26">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 카드 그림자 배경 -->
            <Border Background="White" CornerRadius="20" Effect="{StaticResource ShadowEffect}" Height="700" Margin="0,68,0,0"/>
            <Border Background="White" CornerRadius="20" BorderBrush="#DDDDDD" BorderThickness="1" Margin="0,68,0,0">

                <Grid Margin="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="375"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- 내용 스크롤 -->
                    <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" RenderTransformOrigin="0.503,0.817" Margin="0,0,0,59" Grid.RowSpan="2">
                        <StackPanel>



                            

                            <StackPanel>

                                <!-- 상단 텍스트박스 -->
                                <TextBlock FontSize="21" FontWeight="Bold" Margin="10,10,0,10" TextAlignment="Left"><Run Language="ko-kr" Text="공지사항 제목"/></TextBlock>
                                <Grid  Margin="10,10,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" Foreground="Gray" Text="작성자"/>
                                    <TextBlock Grid.Column="1" Foreground="Gray" Text="2025.08.06" HorizontalAlignment="Right"/>
                                </Grid>

                                <Border Height="3" Background="#E6E6E6" Margin="0,0,0,8"/>

                            </StackPanel>
                            <!-- 내용 -->
                            <TextBlock Grid.Row="2" Text="공지사항 내용" TextAlignment="Center" VerticalAlignment="Center" Margin="0,20,0,0" Height="375"/>


                            <Border Height="3" Background="#E6E6E6" Margin="0,0,0,8"/>
                            <!-- 첨부 자료 -->
                            <Grid Grid.Row="3" Margin="0,20,0,0">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <TextBlock Grid.Row="0" Text="첨부 자료" FontWeight="Bold" Margin="0,0,0,10"/>
                                <ItemsControl Grid.Row="1">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Vertical"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>



                        
                                    
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    
                                                </Grid.ColumnDefinitions>
                                                <!-- 아이콘 -->
                                                <Image Grid.Column="0" Source="{Binding IconSource}" Width="16" Height="16" Margin="0,0,10,0"/>
                                                <!-- 파일 이름 -->
                                                <TextBlock Grid.Column="1" Text="{Binding FileName}" VerticalAlignment="Center"/>
                                                <!-- 다운로드 버튼 -->
                                                <Button Grid.Column="2" Content="↓" Width="20" Height="20" Margin="10,0,0,0" Click="DownloadButton_Click"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                    <!-- 샘플 데이터 (실제 데이터 소스에 맞게 조정 필요) -->
                                    <StackPanel>
                                   
                                    </StackPanel>
                                </ItemsControl>
                            </Grid>







                           

                            <!-- 카드 안쪽 하단 여백 -->
                            <Border Height="8" Background="Transparent"/>
                        </StackPanel>
                    </ScrollViewer>

                </Grid>
            </Border>
        </Grid>
    </Grid>

</Page>